{
  "name": "tezosx-contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:escrow:tezosx": "hardhat run scripts/deploy-escrow.ts --network tezosx",
    "deploy:escrow:local": "hardhat run scripts/deploy-escrow.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17",
    "typescript": "^5.7.2"
  }
}
